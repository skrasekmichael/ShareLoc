<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:ShareLoc.Client.App.Views"
             xmlns:viewModels="clr-namespace:ShareLoc.Client.App.ViewModels"
             x:DataType="viewModels:GuessingPageViewModel"
             x:Class="ShareLoc.Client.App.Views.Pages.GuessingPage"
             Title="Guess the location">
    <Grid>
        <!--<WebView Source="{Binding Uri}"
                 Cookies="{Binding Cookies}"
                 VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand"/>-->

        <ContentView Content="{Binding WebView}" />
    </Grid>
</ContentPage>
