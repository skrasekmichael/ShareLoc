<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:ShareLoc.Client.App.ViewModels"
             xmlns:models="clr-namespace:ShareLoc.Client.App.Models"
             xmlns:views="clr-namespace:ShareLoc.Client.App.Views"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:DataType="viewModels:PlaceDetailPageViewModel"
             x:Class="ShareLoc.Client.App.Views.Pages.PlaceDetailPage"
             Title="Place Detail">
    <Grid VerticalOptions="Fill">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <views:PlaceDetailView Grid.Row="0" BindingContext="{Binding PlaceDetailViewModel}" />

        <Grid Grid.Row="1" HorizontalOptions="Fill">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Text="Share" Margin="2" Command="{Binding ShareUrlCommand}"/>
            <Button Grid.Column="1" Text="Delete" Margin="2" Command="{Binding DeleteCommand}"/>
        </Grid>
    </Grid>
</ContentPage>
