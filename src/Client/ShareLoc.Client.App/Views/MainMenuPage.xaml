<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:ShareLoc.Client.App.ViewModels"
             x:DataType="viewModels:MainMenuViewModel"
             x:Class="ShareLoc.Client.App.Views.MainMenuPage">

    <ScrollView>
        <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center">
            <Image Source="dotnet_bot.png" HeightRequest="200" HorizontalOptions="Center"/>
            <VerticalStackLayout MaximumWidthRequest="300">
                <Button Text="Create Place" Command="{Binding GoToCreatePlacePageCommand}"/>
                <Button Text="Gallery" Command="{Binding GoToGalleryPageCommand}"/>
                <Button Text="My Guesses" Command="{Binding GoToMyGuessesPageCommand}"/>
                <Label>
                    <Label.FormattedText>
                        <FormattedString>
                            <Span Text="Version: "/>
                            <Span Text="{Binding AppVersion}"/>
                        </FormattedString>
                    </Label.FormattedText>
                </Label>
            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
